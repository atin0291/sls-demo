type Post {
  id: ID!
  title: String!
  content: String
}

type Query {
  getPost(id: ID!): Post
}

type Mutation {
  createPost(id: ID!, title: String!, content: String): Post
}

schema {
  query: Query
  mutation: Mutation
}
